<!-- Org Scaffold: reference/validated-facts-org.md
     Scope: org-level coordination
     Source: generated by step-11-org-scaffold
     See: D80-org-coordination.md for full specification -->

<!-- Scope: org-level. Per-project equivalent: [project]/.codex/reference/validated-facts.md -->

# Org Validated Facts

<!-- Org-level facts validated against authoritative sources.
     Evidence required for each entry. Per-project facts belong in each project's .codex/reference/.
     See D80-org-coordination.md § Reference File Scoping for the org/project boundary rule.

     Graduation rule: a fact validated in one project becomes org-wide when confirmed across
     2+ member projects. Move here with provenance noting which projects confirmed it. -->

## Authenticated Services

<!-- Org-wide service registry: APIs, databases, messaging platforms that all projects may interact with.
     Per-project services that have not been confirmed across 2+ projects belong in the project's
     own .codex/reference/validated-facts.md, NOT here.
     Format: Service Name | Base URL/endpoint | Auth method | Validated date | Confirmed by (projects) -->

## Shared Infrastructure Constants

<!-- Values that are true across all deployments: region, cluster names, shared config keys.
     Per-project constants that have not been confirmed across 2+ projects belong in the project's
     own .codex/reference/validated-facts.md, NOT here.
     Format: Key | Value | Source | Validated date | Confirmed by (projects) -->

## API Baselines

<!-- Known-good API behaviors, rate limits, pagination patterns validated empirically across 2+ projects.
     Single-project observations belong in the project's own .codex/reference/validated-facts.md.
     Format: Service | Endpoint | Behavior | Validated date | Evidence | Confirmed by (projects) -->
